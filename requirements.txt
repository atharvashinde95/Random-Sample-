# ============================================================
#  Employee Helpdesk AI — requirements
#
#  IMPORTANT: langchain==1.0.0 does NOT exist on PyPI.
#  The 1.x line starts at 1.0.3 (released Oct 2025).
#
#  Run this FIRST before streamlit run app.py:
#
#  Windows (fresh venv):
#    deactivate
#    rmdir /s /q .venv
#    python -m venv .venv
#    .venv\Scripts\activate
#    pip install -r requirements.txt
#
#  Mac/Linux (fresh venv):
#    deactivate && rm -rf .venv
#    python -m venv .venv && source .venv/bin/activate
#    pip install -r requirements.txt
#
#  Then run:
#    streamlit run app.py
# ============================================================

# ── LangChain ecosystem ──────────────────────────────────────
langchain>=1.0.3
langchain-openai>=1.0.3
langchain-core>=1.0.0
langgraph>=1.0.0

# ── OpenAI HTTP client ───────────────────────────────────────
openai>=1.58.0,<2.0.0

# ── UI ───────────────────────────────────────────────────────
streamlit>=1.40.0,<2.0.0

# ── Utilities ────────────────────────────────────────────────
python-dotenv>=1.0.0,<2.0.0
